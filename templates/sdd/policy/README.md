# Policy‑as‑code

Здесь лежат примеры машиночитаемых политик.

- `gates.example.json` — пример описания quality gates.

В реальном проекте политики могут расширяться:

- схемы документов (JSON Schema),
- правила зависимостей,
- правила секретов,
- правила экспорта/санитизации.
